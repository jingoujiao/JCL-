<Window x:Class="MinecraftLuanch.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="JCL 启动器" Height="650" Width="1100" MinHeight="550" MinWidth="900"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResize"
        Icon="/Assets/Icons/icon.ico"
        x:Name="MainWin">
    
    <Window.Resources>
        <Storyboard x:Key="JellyAnimation" RepeatBehavior="Forever">
            <DoubleAnimationUsingKeyFrames Storyboard.TargetName="JellyIcon" 
                                           Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)">
                <EasingDoubleKeyFrame KeyTime="0:0:0" Value="1"/>
                <EasingDoubleKeyFrame KeyTime="0:0:0.15" Value="1.15">
                    <EasingDoubleKeyFrame.EasingFunction>
                        <ElasticEase Oscillations="3" Springiness="8"/>
                    </EasingDoubleKeyFrame.EasingFunction>
                </EasingDoubleKeyFrame>
                <EasingDoubleKeyFrame KeyTime="0:0:0.5" Value="1"/>
                <EasingDoubleKeyFrame KeyTime="0:0:2" Value="1"/>
                <EasingDoubleKeyFrame KeyTime="0:0:2.15" Value="1.15">
                    <EasingDoubleKeyFrame.EasingFunction>
                        <ElasticEase Oscillations="3" Springiness="8"/>
                    </EasingDoubleKeyFrame.EasingFunction>
                </EasingDoubleKeyFrame>
                <EasingDoubleKeyFrame KeyTime="0:0:2.5" Value="1"/>
            </DoubleAnimationUsingKeyFrames>
            <DoubleAnimationUsingKeyFrames Storyboard.TargetName="JellyIcon" 
                                           Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)">
                <EasingDoubleKeyFrame KeyTime="0:0:0" Value="1"/>
                <EasingDoubleKeyFrame KeyTime="0:0:0.15" Value="0.85">
                    <EasingDoubleKeyFrame.EasingFunction>
                        <ElasticEase Oscillations="3" Springiness="8"/>
                    </EasingDoubleKeyFrame.EasingFunction>
                </EasingDoubleKeyFrame>
                <EasingDoubleKeyFrame KeyTime="0:0:0.5" Value="1"/>
                <EasingDoubleKeyFrame KeyTime="0:0:2" Value="1"/>
                <EasingDoubleKeyFrame KeyTime="0:0:2.15" Value="0.85">
                    <EasingDoubleKeyFrame.EasingFunction>
                        <ElasticEase Oscillations="3" Springiness="8"/>
                    </EasingDoubleKeyFrame.EasingFunction>
                </EasingDoubleKeyFrame>
                <EasingDoubleKeyFrame KeyTime="0:0:2.5" Value="1"/>
            </DoubleAnimationUsingKeyFrames>
        </Storyboard>
        
        <Style x:Key="NavBarButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#FFFFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" 
                                CornerRadius="8" Padding="12,8" Margin="2"
                                RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform>
                                <ScaleTransform ScaleX="1" ScaleY="1"/>
                            </Border.RenderTransform>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock x:Name="IconText" Text="{TemplateBinding Tag}" 
                                          FontSize="18" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock x:Name="ButtonText" Text="{TemplateBinding Content}" 
                                          VerticalAlignment="Center" FontWeight="Medium"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#33FFFFFF"/>
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="ButtonBorder" 
                                                            Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                            From="1" To="1.05" Duration="0:0:0.15"/>
                                            <DoubleAnimation Storyboard.TargetName="ButtonBorder" 
                                                            Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                            From="1" To="1.05" Duration="0:0:0.15"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="ButtonBorder" 
                                                            Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                            From="1.05" To="1" Duration="0:0:0.15"/>
                                            <DoubleAnimation Storyboard.TargetName="ButtonBorder" 
                                                            Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                            From="1.05" To="1" Duration="0:0:0.15"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="ActiveNavBarButton" TargetType="Button" BasedOn="{StaticResource NavBarButton}">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" 
                                CornerRadius="8" Padding="12,8" Margin="2"
                                RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform>
                                <ScaleTransform ScaleX="1" ScaleY="1"/>
                            </Border.RenderTransform>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock x:Name="IconText" Text="{TemplateBinding Tag}" 
                                          FontSize="18" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock x:Name="ButtonText" Text="{TemplateBinding Content}" 
                                          VerticalAlignment="Center" FontWeight="Medium"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="ButtonBorder" 
                                                            Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                            From="1" To="1.05" Duration="0:0:0.15"/>
                                            <DoubleAnimation Storyboard.TargetName="ButtonBorder" 
                                                            Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                            From="1" To="1.05" Duration="0:0:0.15"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="ButtonBorder" 
                                                            Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                            From="1.05" To="1" Duration="0:0:0.15"/>
                                            <DoubleAnimation Storyboard.TargetName="ButtonBorder" 
                                                            Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                            From="1.05" To="1" Duration="0:0:0.15"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="ModernCard" TargetType="Border">
            <Setter Property="Background" Value="#40FFFFFF"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.1" Direction="270"/>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="DarkModernCard" TargetType="Border">
            <Setter Property="Background" Value="#402D2D30"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.2" Direction="270"/>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="GlassCard" TargetType="Border">
            <Setter Property="Background" Value="#30FFFFFF"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="BorderBrush" Value="#40FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" 
                                CornerRadius="10" Padding="30,15">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#66BB6A"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#43A047"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#BDBDBD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" 
                                CornerRadius="8" Padding="20,10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#42A5F5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="DangerButton" TargetType="Button">
            <Setter Property="Background" Value="#F44336"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" 
                                CornerRadius="8" Padding="20,10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#EF5350"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="#30FFFFFF"/>
            <Setter Property="BorderBrush" Value="#40FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Height" Value="42"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="MainBorder" Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="8" Cursor="Hand"
                                    MouseLeftButtonDown="ComboBoxBorder_MouseLeftButtonDown"/>
                            <ContentPresenter x:Name="ContentSite" 
                                              VerticalAlignment="Center" 
                                              HorizontalAlignment="Left" 
                                              Margin="12,0,30,0"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              TextElement.Foreground="White"/>
                            <Path x:Name="Arrow" Data="M0,0 L4,4 L8,0" Stroke="White" 
                                  StrokeThickness="2" HorizontalAlignment="Right" 
                                  VerticalAlignment="Center" Margin="0,0,12,0"/>
                            <Popup x:Name="Popup" Placement="Bottom" 
                                   IsOpen="{TemplateBinding IsDropDownOpen}" 
                                   AllowsTransparency="True" PopupAnimation="Slide"
                                   MinWidth="{TemplateBinding ActualWidth}">
                                <Border Background="#CC1A1A1A" CornerRadius="8" Margin="0,2,0,0" 
                                        MaxHeight="300">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="15" ShadowDepth="3" Opacity="0.3"/>
                                    </Border.Effect>
                                    <ScrollViewer>
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Resources>
                <Style TargetType="ComboBoxItem">
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Padding" Value="12,10"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ComboBoxItem">
                                <Border x:Name="ItemBorder" Background="{TemplateBinding Background}" 
                                        CornerRadius="6" Margin="4,2">
                                    <ContentPresenter VerticalAlignment="Center" Margin="8,6"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="ItemBorder" Property="Background" Value="#30FFFFFF"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="ItemBorder" Property="Background" Value="#404CAF50"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Style.Resources>
        </Style>
        
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#30FFFFFF"/>
            <Setter Property="BorderBrush" Value="#40FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#4CAF50"/>
                                <Setter TargetName="Border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="ModernProgressBar" TargetType="ProgressBar">
            <Setter Property="Height" Value="8"/>
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="Foreground" Value="#4CAF50"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <Border x:Name="PART_Indicator" 
                                    Background="{TemplateBinding Foreground}" 
                                    CornerRadius="4"
                                    HorizontalAlignment="Left"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="VersionListItem" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="8" ShadowDepth="1" Opacity="0.1"/>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="WindowTitleButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#33FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="CloseButton" TargetType="Button" BasedOn="{StaticResource WindowTitleButton}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E81123"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid x:Name="BackgroundGrid">
            <Image x:Name="BackgroundImage" Stretch="UniformToFill" 
                   RenderOptions.BitmapScalingMode="HighQuality"/>
            <Border Background="#33000000"/>
        </Grid>
        
        <Border x:Name="MainBorder" Background="Transparent" 
                CornerRadius="12" Margin="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <Border Grid.Row="0" x:Name="TitleBar" Background="#00000000" 
                        CornerRadius="12,12,0,0" Height="40" 
                        MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                    <Grid>
                        <StackPanel Orientation="Horizontal" Margin="16,0,0,0" VerticalAlignment="Center">
                            <Grid x:Name="JellyIcon" Width="24" Height="24" Margin="0,0,10,0"
                                  RenderTransformOrigin="0.5,0.5">
                                <Grid.RenderTransform>
                                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                                </Grid.RenderTransform>
                                <Ellipse Fill="#4CAF50" Stroke="#2E7D32" StrokeThickness="1"/>
                                <Ellipse Fill="#66BB6A" Margin="4,2,8,10" Opacity="0.6"/>
                                <TextBlock Text="⛏" FontSize="12" Foreground="White" 
                                          HorizontalAlignment="Center" VerticalAlignment="Center"
                                          FontWeight="Bold"/>
                            </Grid>
                            <TextBlock Text="JCL 启动器" FontSize="14" FontWeight="Medium" 
                                      Foreground="White" VerticalAlignment="Center"/>
                            <TextBlock x:Name="VersionText" Text="v1.2.2" FontSize="11" 
                                      Foreground="#AAFFFFFF" VerticalAlignment="Center" Margin="10,0,0,0"/>
                        </StackPanel>
                        
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button x:Name="MinimizeButton" Content="─" 
                                    Style="{StaticResource WindowTitleButton}"
                                    Click="MinimizeButton_Click" FontSize="12"/>
                            <Button x:Name="MaximizeButton" Content="□" 
                                    Style="{StaticResource WindowTitleButton}"
                                    Click="MaximizeButton_Click" FontSize="14"/>
                            <Button x:Name="CloseButton" Content="✕" 
                                    Style="{StaticResource CloseButton}"
                                    Click="CloseButton_Click" FontSize="12"/>
                        </StackPanel>
                    </Grid>
                </Border>
                
                <Grid Grid.Row="1" Margin="20,10,20,10">
                    <Grid x:Name="LaunchPage" Visibility="Visible">
                        <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Left" 
                                    Orientation="Vertical" Margin="20,0,0,20">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <Button x:Name="StartGame" Grid.Column="0" 
                                        Content="🚀 开始游戏" 
                                        Background="#404CAF50" Foreground="White"
                                        Height="48" FontSize="15" FontWeight="SemiBold"
                                        BorderThickness="0" Cursor="Hand" Click="StartGame_Click">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" 
                                                    CornerRadius="10,0,0,10" Padding="20,0">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="ButtonBorder" Property="Background" Value="#6066BB6A"/>
                                                </Trigger>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter TargetName="ButtonBorder" Property="Background" Value="#6043A047"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                                
                                <Button x:Name="VersionDropDown" Grid.Column="1" 
                                        Content="▲" Width="40" Height="48" Margin="0,0,0,0"
                                        Background="#40FFFFFF" Foreground="White" FontSize="14"
                                        BorderThickness="0" Cursor="Hand" Click="VersionDropDown_Click">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" 
                                                    CornerRadius="0,10,10,0">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="ButtonBorder" Property="Background" Value="#60FFFFFF"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                            </Grid>
                            
                            <TextBlock x:Name="SelectedVersionText" Text="当前版本: 未选择" 
                                      FontSize="12" Foreground="#CCFFFFFF" Margin="5,8,0,0"/>
                            
                            <ProgressBar x:Name="LaunchProgress" Style="{StaticResource ModernProgressBar}"
                                         Visibility="Collapsed" Margin="0,8,0,0" Width="200" Height="8"
                                         Foreground="#4CAF50" Background="#40FFFFFF" Minimum="0" Maximum="100" Value="0"/>
                            
                            <TextBlock x:Name="LaunchStatusText" Text="" FontSize="11" 
                                      Foreground="#CCFFFFFF" Margin="5,5,0,0" Visibility="Collapsed"/>
                        </StackPanel>
                        
                        <Popup x:Name="VersionPopup" PlacementTarget="{Binding ElementName=VersionDropDown}" 
                               Placement="Top" StaysOpen="True" AllowsTransparency="True"
                               PopupAnimation="Slide" Width="280">
                            <Border Background="#E0FFFFFF" CornerRadius="12" Padding="8" Margin="0,0,0,8"
                                    MaxHeight="300">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="20" ShadowDepth="2" Opacity="0.15"/>
                                </Border.Effect>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    
                                    <DockPanel Grid.Row="0" Margin="6,4,6,6">
                                        <Button x:Name="RefreshGameVersionsBtn" Content="🔄" 
                                                Width="32" Height="28" Margin="0,0,6,0"
                                                Background="#F0F0F0" BorderThickness="0" 
                                                Click="RefreshVersions_Click" Cursor="Hand">
                                            <Button.Template>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Button.Template>
                                        </Button>
                                        <TextBox x:Name="VersionSearchBox" 
                                                 Style="{StaticResource ModernTextBox}"
                                                 Tag="搜索版本..." TextChanged="VersionSearchBox_TextChanged"/>
                                    </DockPanel>
                                    
                                    <ListBox Grid.Row="1" x:Name="VersionListBox" 
                                             BorderThickness="0" MaxHeight="200"
                                             Background="Transparent"
                                             SelectionChanged="VersionListBox_SelectionChanged">
                                        <ListBox.ItemTemplate>
                                            <DataTemplate>
                                                <Border Padding="10,6" CornerRadius="6">
                                                    <TextBlock Text="{Binding}" FontSize="13"/>
                                                </Border>
                                            </DataTemplate>
                                        </ListBox.ItemTemplate>
                                        <ListBox.ItemContainerStyle>
                                            <Style TargetType="ListBoxItem">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="ListBoxItem">
                                                            <Border x:Name="ItemBorder" Background="Transparent" CornerRadius="6">
                                                                <ContentPresenter/>
                                                            </Border>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter TargetName="ItemBorder" Property="Background" Value="#20000000"/>
                                                                </Trigger>
                                                                <Trigger Property="IsSelected" Value="True">
                                                                    <Setter TargetName="ItemBorder" Property="Background" Value="#404CAF50"/>
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </ListBox.ItemContainerStyle>
                                    </ListBox>
                                </Grid>
                            </Border>
                        </Popup>
                    </Grid>
                    
                    <Grid x:Name="VersionsPage" Visibility="Collapsed">
                        <Border Style="{StaticResource ModernCard}" Padding="30" Margin="40,20">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                
                                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
                                    <TextBlock Text="📦 版本管理" FontSize="24" FontWeight="Bold" Foreground="White"/>
                                    <Button x:Name="RefreshVersionsListBtn" Content="🔄 刷新" 
                                            Style="{StaticResource SecondaryButton}" Margin="20,0,0,0"
                                            Click="RefreshVersionsListButton_Click"/>
                                    <Button x:Name="ImportVersionBtn" Content="📥 导入版本" 
                                            Style="{StaticResource SecondaryButton}" Margin="10,0,0,0"
                                            Click="ImportVersionButton_Click"/>
                                </StackPanel>
                                
                                <TextBlock Grid.Row="1" Text="已安装的游戏版本" FontSize="14" 
                                          Foreground="#CCFFFFFF" Margin="0,0,0,15"/>
                                
                                <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                                    <ItemsControl x:Name="VersionsList">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="#30FFFFFF" CornerRadius="10" Padding="16" Margin="0,0,0,8">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        
                                                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                                            <TextBlock Text="{Binding VersionName}" FontSize="15" 
                                                                      FontWeight="SemiBold" Foreground="White"/>
                                                            <TextBlock Text="{Binding VersionPath}" FontSize="11" 
                                                                      Foreground="#AAFFFFFF" Margin="0,4,0,0"/>
                                                        </StackPanel>
                                                        
                                                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                                                            <Button Content="📂" Width="40" Height="36" Margin="0,0,6,0"
                                                                    Background="#40FF9800" Foreground="White" 
                                                                    BorderThickness="0" FontSize="16" Cursor="Hand"
                                                                    Click="OpenVersionFolder_Click" Tag="{Binding VersionName}">
                                                                <Button.Template>
                                                                    <ControlTemplate TargetType="Button">
                                                                        <Border x:Name="Btn" Background="{TemplateBinding Background}" CornerRadius="8">
                                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                        </Border>
                                                                        <ControlTemplate.Triggers>
                                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                                <Setter TargetName="Btn" Property="Background" Value="#60FF9800"/>
                                                                            </Trigger>
                                                                        </ControlTemplate.Triggers>
                                                                    </ControlTemplate>
                                                                </Button.Template>
                                                            </Button>
                                                            <Button Content="✏️" Width="40" Height="36" Margin="0,0,6,0"
                                                                    Background="#402196F3" Foreground="White" 
                                                                    BorderThickness="0" FontSize="14" Cursor="Hand"
                                                                    Click="RenameVersion_Click" Tag="{Binding VersionName}">
                                                                <Button.Template>
                                                                    <ControlTemplate TargetType="Button">
                                                                        <Border x:Name="Btn" Background="{TemplateBinding Background}" CornerRadius="8">
                                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                        </Border>
                                                                        <ControlTemplate.Triggers>
                                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                                <Setter TargetName="Btn" Property="Background" Value="#602196F3"/>
                                                                            </Trigger>
                                                                        </ControlTemplate.Triggers>
                                                                    </ControlTemplate>
                                                                </Button.Template>
                                                            </Button>
                                                            <Button Content="🗑️" Width="40" Height="36"
                                                                    Background="#40F44336" Foreground="White" 
                                                                    BorderThickness="0" FontSize="14" Cursor="Hand"
                                                                    Click="DeleteVersion_Click" Tag="{Binding VersionName}">
                                                                <Button.Template>
                                                                    <ControlTemplate TargetType="Button">
                                                                        <Border x:Name="Btn" Background="{TemplateBinding Background}" CornerRadius="8">
                                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                        </Border>
                                                                        <ControlTemplate.Triggers>
                                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                                <Setter TargetName="Btn" Property="Background" Value="#60F44336"/>
                                                                            </Trigger>
                                                                        </ControlTemplate.Triggers>
                                                                    </ControlTemplate>
                                                                </Button.Template>
                                                            </Button>
                                                        </StackPanel>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>
                            </Grid>
                        </Border>
                    </Grid>
                    
                    <Grid x:Name="DownloadPage" Visibility="Collapsed">
                        <Border Style="{StaticResource ModernCard}" Padding="30" Margin="40,20">
                            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                <StackPanel>
                                
                                <TextBlock Text="📥 下载中心" FontSize="24" 
                                          FontWeight="Bold" Foreground="White" Margin="0,0,0,20"/>

                                <Border Background="#252196F3" CornerRadius="12" Padding="20" Margin="0,0,0,15">
                                    <StackPanel>
                                        <TextBlock Text="🎮 游戏版本下载" FontSize="18" FontWeight="SemiBold"
                                                  Foreground="White" Margin="0,0,0,12"/>
                                        <TextBlock Text="下载并安装 Minecraft 游戏版本，支持正式版和测试版。"
                                                  FontSize="12" Foreground="#CCFFFFFF" Margin="0,0,0,15"/>

                                        <Grid Margin="0,0,0,15">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                                <TextBlock Text="版本类型" FontSize="12" Foreground="#CCFFFFFF" Margin="0,0,0,6"/>
                                                <ComboBox x:Name="VersionType" Style="{StaticResource ModernComboBox}"
                                                          Height="36"
                                                          SelectionChanged="VersionType_SelectionChanged">
                                                    <ComboBoxItem Content="✨ 正式版"/>
                                                    <ComboBoxItem Content="🔬 测试版"/>
                                                </ComboBox>
                                            </StackPanel>
                                            
                                            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                                <TextBlock Text="选择版本" FontSize="12" Foreground="#CCFFFFFF" Margin="0,0,0,6"/>
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <ComboBox Grid.Column="0" x:Name="InstallVersion" 
                                                              Style="{StaticResource ModernComboBox}"
                                                              Height="36"/>
                                                    <Button Grid.Column="1" x:Name="RefreshVersionsBtn" Content="🔄" 
                                                            Width="36" Height="36" Margin="6,0,0,0"
                                                            Background="#30FFFFFF" Foreground="White" BorderThickness="0" 
                                                            Click="RefreshVersionsButton_Click" Cursor="Hand">
                                                        <Button.Template>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border x:Name="Btn" Background="{TemplateBinding Background}" CornerRadius="6">
                                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                </Border>
                                                                <ControlTemplate.Triggers>
                                                                    <Trigger Property="IsMouseOver" Value="True">
                                                                        <Setter TargetName="Btn" Property="Background" Value="#50FFFFFF"/>
                                                                    </Trigger>
                                                                </ControlTemplate.Triggers>
                                                            </ControlTemplate>
                                                        </Button.Template>
                                                    </Button>
                                                </Grid>
                                            </StackPanel>
                                        </Grid>

                                        <Border x:Name="GameDownloadProgressPanel" Background="#15FFFFFF" CornerRadius="8" Padding="12" Margin="0,0,0,15" Visibility="Collapsed">
                                            <StackPanel>
                                                <ProgressBar x:Name="DownloadProgressBar" 
                                                            Style="{StaticResource ModernProgressBar}" Height="10"
                                                            Foreground="#2196F3" Background="#30FFFFFF"
                                                            Minimum="0" Maximum="100" Value="0"/>
                                            
                                                <Grid Margin="0,8,0,0">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Grid.Column="0" x:Name="DownloadPercentText" 
                                                              Text="0%" FontSize="13" FontWeight="SemiBold" Foreground="White"/>
                                                    <TextBlock Grid.Column="1" x:Name="DownloadSizeText" 
                                                              Text="" FontSize="12" Foreground="#AAFFFFFF" HorizontalAlignment="Center"/>
                                                    <TextBlock Grid.Column="2" x:Name="DownloadEtaText" 
                                                              Text="剩余: --" FontSize="12" Foreground="#AAFFFFFF"/>
                                                </Grid>
                                            
                                                <Grid Margin="0,4,0,0">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Grid.Column="0" x:Name="DownloadSourceText" 
                                                              Text="" FontSize="11" Foreground="#88FFFFFF"/>
                                                    <TextBlock Grid.Column="1" x:Name="DownloadSpeedText" 
                                                              Text="速度: --" FontSize="13" FontWeight="SemiBold" Foreground="#4CAF50" HorizontalAlignment="Center"/>
                                                    <TextBlock Grid.Column="2" x:Name="DownloadStatusText" 
                                                              Text="" FontSize="11" Foreground="#88FFFFFF"/>
                                                </Grid>
                                            </StackPanel>
                                        </Border>
                                        
                                        <TextBlock x:Name="DownloadProgressText" 
                                                  Text="准备就绪" FontSize="12" Foreground="#AAFFFFFF" Margin="0,0,0,15" Visibility="Collapsed"/>
                                        
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <Button Grid.Column="0" x:Name="InstallVanillaBtn" 
                                                    Content="📥 开始下载" Style="{StaticResource SecondaryButton}"
                                                    Click="InstallVanilla_Click" HorizontalAlignment="Left"/>
                                            
                                            <Button Grid.Column="1" x:Name="CancelDownloadBtn" 
                                                    Content="❌ 取消" Style="{StaticResource DangerButton}"
                                                    Margin="10,0,0,0"
                                                    Visibility="Collapsed" Click="CancelDownload_Click"/>
                                        </Grid>
                                    </StackPanel>
                                </Border>

                                <Border Background="#25FF9800" CornerRadius="12" Padding="20" Margin="0,0,0,15">
                                    <StackPanel>
                                        <TextBlock Text="☕ Java 下载与管理" FontSize="18" FontWeight="SemiBold"
                                                  Foreground="White" Margin="0,0,0,12"/>
                                        <TextBlock Text="下载、配置和管理 Java 运行环境，游戏启动前请确保 Java 可用。"
                                                  FontSize="12" Foreground="#CCFFFFFF" Margin="0,0,0,15"/>

                                        <TextBlock Text="当前 Java 路径（启动游戏时使用）" FontSize="12" Foreground="#CCFFFFFF" Margin="0,0,0,6"/>
                                        <Grid Margin="0,0,0,6">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <ComboBox Grid.Column="0" x:Name="JavaPath"
                                                      Style="{StaticResource ModernComboBox}"
                                                      Height="36"
                                                      IsEditable="True"
                                                      SelectionChanged="JavaPath_SelectionChanged"
                                                      LostFocus="JavaPath_LostFocus"/>
                                            <Button Grid.Column="1" x:Name="RefreshJavaBtn" Content="🔄"
                                                    Width="36" Height="36" Margin="6,0,0,0"
                                                    Background="#30FFFFFF" Foreground="White" BorderThickness="0"
                                                    Click="RefreshJava_Click" Cursor="Hand"/>
                                            <Button Grid.Column="2" x:Name="BrowseJavaBtn" Content="📁"
                                                    Width="36" Height="36" Margin="6,0,0,0"
                                                    Background="#30FFFFFF" Foreground="White" BorderThickness="0"
                                                    Click="BrowseJava_Click" Cursor="Hand"/>
                                        </Grid>
                                        <TextBlock Text="🔄 重新扫描本机 Java | 📁 手动指定 java.exe/javaw.exe"
                                                  FontSize="11" Foreground="#AAFFFFFF" Margin="0,0,0,12"/>

                                        <Grid Margin="0,0,0,12">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                                <TextBlock Text="Java 版本" FontSize="12" Foreground="#CCFFFFFF" Margin="0,0,0,6"/>
                                                <ComboBox x:Name="JavaTargetVersion"
                                                          Style="{StaticResource ModernComboBox}"
                                                          Height="36"
                                                          SelectionChanged="JavaTargetVersion_SelectionChanged">
                                                    <ComboBoxItem Content="自动（按游戏版本）" Tag="auto"/>
                                                    <ComboBoxItem Content="Java 8（1.16及以下）" Tag="8"/>
                                                    <ComboBoxItem Content="Java 17（1.17-1.20）" Tag="17"/>
                    <ComboBoxItem Content="Java 21（1.21+）" Tag="21"/>
                                                </ComboBox>
                                            </StackPanel>
                                            
                                            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                                <TextBlock Text="下载源" FontSize="12" Foreground="#CCFFFFFF" Margin="0,0,0,6"/>
                                                <ComboBox x:Name="JavaDownloadSource"
                                                          Style="{StaticResource ModernComboBox}"
                                                          Height="36"
                                                          SelectionChanged="JavaDownloadSource_SelectionChanged">
                                                    <ComboBoxItem Content="清华镜像（推荐）" Tag="tuna"/>
                    <ComboBoxItem Content="官方源" Tag="official"/>
                                                </ComboBox>
                                            </StackPanel>
                                        </Grid>
                                        <TextBlock x:Name="JavaVersionHintText"
                                                  Text="建议：1.21+ 用 Java 21，1.17-1.20 用 Java 17，1.16 及以下用 Java 8"
                                                  FontSize="11"
                                                  Foreground="#AAFFFFFF"
                                                  Margin="0,0,0,12"/>

                                        <Button x:Name="AutoInstallJavaBtn"
                                                Content="⬇️ 一键下载并配置 Java"
                                                Style="{StaticResource SecondaryButton}"
                                                Click="AutoInstallJava_Click"
                                                HorizontalAlignment="Left"
                                                Margin="0,0,0,10"/>

                                        <Border x:Name="JavaInstallProgressPanel" 
                                                Background="#15FFFFFF" 
                                                CornerRadius="8" 
                                                Padding="12"
                                                Visibility="Collapsed">
                                            <StackPanel>
                                                <ProgressBar x:Name="JavaInstallProgressBar"
                                                             Style="{StaticResource ModernProgressBar}"
                                                             Height="10"
                                                             Minimum="0"
                                                             Maximum="100"
                                                             Value="0"/>
                                            
                                                <Grid Margin="0,8,0,0">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Grid.Column="0" x:Name="JavaInstallPercentText"
                                                               Text="0%"
                                                               FontSize="13"
                                                               FontWeight="SemiBold"
                                                               Foreground="White"/>
                                                    <TextBlock Grid.Column="1" x:Name="JavaInstallSizeText"
                                                               Text=""
                                                               FontSize="12"
                                                               Foreground="#AAFFFFFF"
                                                               HorizontalAlignment="Center"/>
                                                    <TextBlock Grid.Column="2" x:Name="JavaInstallEtaText"
                                                               Text="剩余: --"
                                                               FontSize="12"
                                                               Foreground="#AAFFFFFF"/>
                                                </Grid>
                                            
                                                <Grid Margin="0,4,0,0">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Grid.Column="0" x:Name="JavaInstallSourceText"
                                                               Text=""
                                                               FontSize="11"
                                                               Foreground="#88FFFFFF"/>
                                                    <TextBlock Grid.Column="1" x:Name="JavaInstallSpeedText"
                                                               Text="速度: --"
                                                               FontSize="13"
                                                               FontWeight="SemiBold"
                                                               Foreground="#4CAF50"
                                                               HorizontalAlignment="Center"/>
                                                    <TextBlock Grid.Column="2" x:Name="JavaInstallStatusText"
                                                               Text=""
                                                               FontSize="11"
                                                               Foreground="#88FFFFFF"/>
                                                </Grid>
                                            
                                                <Button x:Name="CancelJavaInstallBtn"
                                                        Content="❌ 取消下载"
                                                        Style="{StaticResource DangerButton}"
                                                        Margin="0,10,0,0"
                                                        Click="CancelJavaInstall_Click"
                                                        HorizontalAlignment="Left"/>
                                            </StackPanel>
                                        </Border>
                                    </StackPanel>
                                </Border>

                                </StackPanel>
                            </ScrollViewer>
                        </Border>
                    </Grid>
                    
                    <Grid x:Name="AccountPage" Visibility="Collapsed">
                        <Border Style="{StaticResource ModernCard}" Padding="40" 
                                HorizontalAlignment="Center" VerticalAlignment="Center" Width="500">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <TextBlock Grid.Row="0" Text="👤 账号管理" FontSize="24" 
                                          FontWeight="Bold" Foreground="White" Margin="0,0,0,25"/>
                                
                                <Border Grid.Row="1" Background="#30FFFFFF" CornerRadius="12" 
                                        Padding="20" Margin="0,0,0,20">
                                    <StackPanel>
                                        <TextBlock Text="选择登录方式" FontSize="14" FontWeight="SemiBold" 
                                                  Foreground="White" Margin="0,0,0,12"/>
                                        <RadioButton x:Name="OfflineModeRadio" Content="🎮 离线模式（无需正版账号）" 
                                                    FontSize="14" IsChecked="True" Margin="0,0,0,8"
                                                    Checked="OfflineModeRadio_Checked" Foreground="White"/>
                                        <RadioButton x:Name="OnlineModeRadio" Content="🔐 正版验证（微软账号）" 
                                                    FontSize="14" Checked="OnlineModeRadio_Checked" Foreground="White"/>
                                    </StackPanel>
                                </Border>
                                
                                <StackPanel Grid.Row="2" x:Name="OfflineAccountPanel">
                                    <TextBlock Text="离线昵称" FontSize="13" Foreground="#CCFFFFFF" Margin="0,0,0,8"/>
                                    <TextBox x:Name="PlayerName" Style="{StaticResource ModernTextBox}"
                                             Margin="0,0,0,15" LostFocus="PlayerName_LostFocus"
                                             TextChanged="PlayerName_TextChanged"/>
                                    <TextBlock Text="💡 离线模式无法使用皮肤和正版服务器" 
                                              FontSize="12" Foreground="#AAFFFFFF"/>
                                </StackPanel>
                                
                                <StackPanel Grid.Row="2" x:Name="OnlineAccountPanel" Visibility="Collapsed">
                                    <Button x:Name="MicrosoftLoginBtn" Content="🔑 微软账号登录" 
                                            Style="{StaticResource SecondaryButton}"
                                            Click="MicrosoftLoginButton_Click" Margin="0,0,0,15"/>
                                    <TextBlock x:Name="LoginStatusText" Text="" FontSize="13" 
                                              Foreground="#CCFFFFFF" TextWrapping="Wrap"/>
                                </StackPanel>
                                
                                <Border Grid.Row="3" Background="#254CAF50" CornerRadius="12" 
                                        Padding="20" Margin="0,20,0,0">
                                    <StackPanel>
                                        <TextBlock Text="当前账号信息" FontSize="14" FontWeight="SemiBold" 
                                                  Foreground="White" Margin="0,0,0,10"/>
                                        <TextBlock x:Name="CurrentAccountInfo" Text="模式：离线模式" 
                                                  FontSize="13" Foreground="White"/>
                                        <TextBlock x:Name="CurrentPlayerName" Text="昵称：未设置" 
                                                  FontSize="13" Foreground="White" Margin="0,4,0,0"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Border>
                    </Grid>
                    
                    <Grid x:Name="SettingsPage" Visibility="Collapsed">
                        <Border Style="{StaticResource ModernCard}" Padding="30" Margin="40,20">
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <StackPanel>
                                    <TextBlock Text="⚙️ 设置" FontSize="24" FontWeight="Bold" 
                                              Foreground="White" Margin="0,0,0,25"/>
                                    
                                    <Border Background="#20FFFFFF" CornerRadius="12" Padding="20" Margin="0,0,0,15">
                                        <StackPanel>
                                            <TextBlock Text="📁 游戏目录" FontSize="15" FontWeight="SemiBold" 
                                                      Foreground="White" Margin="0,0,0,12"/>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBox Grid.Column="0" x:Name="GameRoot" 
                                                        Style="{StaticResource ModernTextBox}"/>
                                                <Button Grid.Column="1" Content="浏览" 
                                                        Style="{StaticResource SecondaryButton}"
                                                        Click="BrowseRoot_Click" Margin="10,0,0,0"/>
                                            </Grid>
                                        </StackPanel>
                                    </Border>
                                    
                                    <Border Background="#20FFFFFF" CornerRadius="12" Padding="20" Margin="0,0,0,15">
                                        <StackPanel>
                                            <TextBlock Text="💾 内存分配" FontSize="15" FontWeight="SemiBold" 
                                                      Foreground="White" Margin="0,0,0,12"/>
                                            <TextBox x:Name="MaxMemory" Style="{StaticResource ModernTextBox}"
                                                     Text="4096"/>
                                            <TextBlock Text="建议：轻量 2048MB / 中型 4096MB / 大型 8192MB" 
                                                      FontSize="11" Foreground="#AAFFFFFF" Margin="0,8,0,0"/>
                                        </StackPanel>
                                    </Border>
                                    
                                    <Border Background="#20FFFFFF" CornerRadius="12" Padding="20" Margin="0,0,0,15">
                                        <StackPanel>
                                            <TextBlock Text="🖼️ 背景图片" FontSize="15" FontWeight="SemiBold" 
                                                      Foreground="White" Margin="0,0,0,12"/>
                                            <TextBlock x:Name="BackgroundCountText" Text="当前背景数量：8 张" 
                                                      FontSize="13" Foreground="#CCFFFFFF" Margin="0,0,0,10"/>
                                            <StackPanel Orientation="Horizontal">
                                                <Button Content="➕ 添加背景" Style="{StaticResource SecondaryButton}"
                                                        Click="AddBackground_Click"/>
                                                <Button Content="🗑️ 清空自定义背景" Style="{StaticResource DangerButton}"
                                                        Margin="10,0,0,0" Click="ClearBackgrounds_Click"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>
                                    
                                    <Border Background="#20FFFFFF" CornerRadius="12" Padding="20" Margin="0,0,0,15">
                                        <StackPanel>
                                            <TextBlock Text="🎬 动画设置" FontSize="15" FontWeight="SemiBold" 
                                                      Foreground="White" Margin="0,0,0,12"/>
                                            <TextBlock Text="动画速度" FontSize="13" Foreground="#CCFFFFFF" Margin="0,0,0,8"/>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <Slider x:Name="AnimationSpeedSlider" Grid.Column="0"
                                                        Minimum="0.5" Maximum="2" Value="1" 
                                                        TickFrequency="0.1" IsSnapToTickEnabled="True"
                                                        VerticalAlignment="Center"
                                                        ValueChanged="AnimationSpeedSlider_ValueChanged"/>
                                                <TextBlock Grid.Column="1" x:Name="AnimationSpeedText" 
                                                           Text="1.0x" FontSize="13" Foreground="White" 
                                                           VerticalAlignment="Center" Margin="15,0,0,0" Width="40"/>
                                            </Grid>
                                            <TextBlock Text="💡 数值越小动画越快，越大越慢" 
                                                      FontSize="11" Foreground="#AAFFFFFF" Margin="0,8,0,0"/>
                                        </StackPanel>
                                    </Border>
                                    
                                    <Border Background="#20FFFFFF" CornerRadius="12" Padding="20" Margin="0,0,0,15">
                                        <StackPanel>
                                            <TextBlock Text="🔧 其他设置" FontSize="15" FontWeight="SemiBold" 
                                                      Foreground="White" Margin="0,0,0,12"/>
                                            <CheckBox x:Name="FullScreen" Content="全屏模式启动" 
                                                     FontSize="13" Foreground="White"/>
                                        </StackPanel>
                                    </Border>
                                    
                                    <Button x:Name="SaveSettingsBtn" Content="💾 保存设置" 
                                            Style="{StaticResource PrimaryButton}"
                                            Click="SaveSettingsButton_Click" HorizontalAlignment="Left"/>
                                </StackPanel>
                            </ScrollViewer>
                        </Border>
                    </Grid>
                    
                    <Grid x:Name="MorePage" Visibility="Collapsed">
                        <Border Style="{StaticResource ModernCard}" Padding="30" Margin="40,20">
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <StackPanel>
                                    <TextBlock Text="ℹ️ 更多" FontSize="24" FontWeight="Bold" 
                                              Foreground="White" Margin="0,0,0,25"/>
                                    
                                    <Border Background="#252196F3" CornerRadius="12" Padding="20" Margin="0,0,0,15">
                                        <StackPanel>
                                            <TextBlock Text="📢 公告" FontSize="15" FontWeight="SemiBold" 
                                                      Foreground="White" Margin="0,0,0,10"/>
                                            <TextBlock x:Name="AnnouncementText" TextWrapping="Wrap" 
                                                      FontSize="13" Foreground="White"/>
                                        </StackPanel>
                                    </Border>
                                    
                                    <Border Background="#20FFFFFF" CornerRadius="12" Padding="20" Margin="0,0,0,15">
                                        <StackPanel>
                                            <TextBlock Text="🎮 JCL 启动器" FontSize="18" FontWeight="Bold" 
                                                      Foreground="White"/>
                                            <TextBlock Text="基于 StarLight_Core 开发" FontSize="13" 
                                                      Foreground="#CCFFFFFF" Margin="0,4,0,0"/>
                                            <TextBlock Text="开发时间：2026 年 3 月" FontSize="12" 
                                                      Foreground="#AAFFFFFF" Margin="0,4,0,0"/>
                                        </StackPanel>
                                    </Border>
                                    
                                    <Border Background="#25FF9800" CornerRadius="12" Padding="20" Margin="0,0,0,15">
                                        <StackPanel>
                                            <TextBlock Text="👨‍💻 关于作者" FontSize="15" FontWeight="SemiBold" 
                                                      Foreground="White" Margin="0,0,0,10"/>
                                            <TextBlock Text="开发者：jingoujiao" FontSize="13" Foreground="White"/>
                                            <TextBlock Text="GitHub: jingoujiao" FontSize="12" 
                                                      Foreground="#CCFFFFFF" Margin="0,4,0,0"/>
                                            <TextBlock Text="QQ: 1494959379" FontSize="12" 
                                                      Foreground="#CCFFFFFF" Margin="0,4,0,0"/>
                                            <Button Content="爱发电支持"
                                                    Style="{StaticResource SecondaryButton}"
                                                    Click="OpenAfdianButton_Click"
                                                    HorizontalAlignment="Left"
                                                    Margin="0,12,0,0"/>
                                        </StackPanel>
                                    </Border>
                                    
                                    <Border Background="#254CAF50" CornerRadius="12" Padding="20">
                                        <StackPanel>
                                            <TextBlock Text="🔧 技术支持" FontSize="15" FontWeight="SemiBold" 
                                                      Foreground="White" Margin="0,0,0,10"/>
                                            <TextBlock Text="如遇问题，请通过以下方式获取帮助：" 
                                                      FontSize="12" Foreground="White" Margin="0,0,0,8"/>
                                            <TextBlock Text="• GitHub Issues: 提交问题反馈" 
                                                      FontSize="12" Foreground="#CCFFFFFF"/>
                                            <TextBlock Text="• 邮箱: jingoujiao@qq.com" 
                                                      FontSize="12" Foreground="#CCFFFFFF"/>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </ScrollViewer>
                        </Border>
                    </Grid>
                </Grid>
                
                <Border Grid.Row="2" x:Name="BottomNavBar" 
                        Background="#CC1A1A1A" CornerRadius="0,0,12,12" Height="70">
                    <Grid Margin="10,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <Button Grid.Column="0" x:Name="LaunchNavBtn" Content="启动" Tag="🚀"
                                Style="{StaticResource ActiveNavBarButton}"
                                Click="LaunchButton_Click"/>
                        <Button Grid.Column="1" x:Name="VersionsNavBtn" Content="版本" Tag="📦"
                                Style="{StaticResource NavBarButton}"
                                Click="VersionsButton_Click"/>
                        <Button Grid.Column="2" x:Name="DownloadNavBtn" Content="下载" Tag="📥"
                                Style="{StaticResource NavBarButton}"
                                Click="DownloadButton_Click"/>
                        <Button Grid.Column="3" x:Name="AccountNavBtn" Content="账号" Tag="👤"
                                Style="{StaticResource NavBarButton}"
                                Click="AccountButton_Click"/>
                        <Button Grid.Column="4" x:Name="SettingsNavBtn" Content="设置" Tag="⚙️"
                                Style="{StaticResource NavBarButton}"
                                Click="SettingsButton_Click"/>
                        <Button Grid.Column="5" x:Name="MoreNavBtn" Content="更多" Tag="ℹ️"
                                Style="{StaticResource NavBarButton}"
                                Click="MoreButton_Click"/>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>
